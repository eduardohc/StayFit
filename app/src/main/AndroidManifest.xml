<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ehernandez.stayfit" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".ParseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name">
        <activity
            android:name=".AuthenticateUser">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterUser"
            android:label="@string/app_name" >
        </activity>

        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme"/>

        <activity android:name=".UserLogin"/>

        <activity android:name=".ResetPassword"/>

        <activity android:name=".Gender_Units"/>

        <activity android:name=".FillWeightHeight"/>

        <activity android:name=".BirthDay"/>

        <activity android:name=".DietGoal"/>

        <activity android:name=".LevelActivity"/>

        <activity android:name=".SelectBirthDate"/>

        <activity android:name=".OpenProgram"/>

        <activity android:name=".DietProgram"/>

        <activity android:name=".ExerciseProgram"/>

    </application>

</manifest>
